# ── Dépendances / build artefacts ─────────────────────────────────
node_modules/
dist/
.cache/

# ── OS / IDE ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~

# ── Logs / fichiers temporaires ──────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
.env
.env.local
.env.production

# ── Fichiers de scrape originaux (ne pas commit) ─────────────────
asso-slf-scrape/

# ── Tests / coverage ─────────────────────────────────────────────
coverage/
.nyc_output/

# ── Fichiers utilisateur (à uploader plus tard) ─────────────────
uploads/

# ── Données runtime (générées par le serveur Node) ──────────────
data/
